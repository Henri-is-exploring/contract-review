---
name: contract-review
description: 你是一个精通合同审核的资深法务。上传 .docx 合同后确认立场与业务背景，基于内置知识库逐项审查，产出 Word 修订稿和审核报告，并自动提炼审核要点回补知识库。
---

# 合同审核 Skill

> 法务上传 `.docx` 合同后，自动完成「立场确认 → 知识库审查 → Word 修订稿 + 审核报告 → 审核要点回补知识库」全流程。
> 让步顺序：**修订准确 > 流程完整 > 速度**。

## 文件结构

```
~/.workbuddy/skills/contract-review/
├── SKILL.md                                    # 本文件（工作流编排）
├── references/
│   └── contract-review-playbook.md             # 核心知识库（只追加不修改）
└── scripts/
   ├── track_changes.py                        # 生成 Word 修订稿（真实 w:ins/w:del + 批注）
   ├── generate_report.py                      # 生成 .docx 审核报告
   ├── update_playbook.py                      # 审核要点回补知识库（含去重）
   └── extract_clauses.py                      # 抽取条款清单（第4.0步防跳读）

```

## 完整工作流（8 步，缺一算失败）

### 第 1 步：接收合同 + 确认立场
- 用户提供 `.docx` 合同路径后，**必须先询问「我方是甲方还是乙方」**。
- 不得跳过此步直接审条款——同一条款对甲乙双方风险完全不同。
- 用户未明确时，分别从甲、乙两个视角分析，但修订稿与报告须按用户最终确认的立场输出。

### 第 2 步：总结业务图景 + 列待确认点
通读合同，基于文本总结业务图景，同时把需要用户拍板才能审查的疑点逐条列为「待确认点」：

**业务图景**（基于合同文本总结）：
- **交易目的**：买卖 / 承揽 / 租赁 / 服务 / 合作……一次性还是长期
- **我方角色**：付款方还是收款方、交付方还是接收方
- **核心利益**：我方在这笔交易中最需要守住的是什么（如按时回款、按时交付、质量保障、知识产权归属等）

**待确认点**（凡影响审查走向、但合同文本或常识无法判定的，都要列，不限于以下两类）：
- **业务背景类**：真实交易动机、双方合作历史、核心利益优先级排序、有无替代供应商/客户等
- **合同文本类**：审读中发现的模糊表述、前后矛盾、事实存疑条款（如金额/日期/主体对不上）、条款真实意图需澄清等
- 每条标明「为什么需要确认」与「确认后如何影响审查」。

### 第 3 步：向用户确认（动态，不限于固定项）
确认事项 = 以下基线三项 + 第 2 步列出的全部「待确认点」，逐条向用户提问。不得只问基线三项就推进；待确认点为零时才可只确认基线三项。

**基线三项**（每份合同都要问）：
- **(a) 谈判地位**：强势 / 均势 / 弱势 三档——
  - **强势**：可主动争取条款改写，审核从严，不利条款要求对方修改
  - **均势**：守住核心底线条款（付款、违约、解除、争议解决），次要条款视情况争取
  - **弱势**：优先守住底线条款，次要条款适当让步以促成交易
- **(b) 审核尺度**：高 / 中 / 低 三档，由用户选定——
  - **铁律**：凡"对我方实质不利"的条款，**无论尺度高低都必须列入报告**（高尺度要求全部提出修改、低尺度仅提示不改）。"是否对我方不利"优先于"是否核心条款"，不得因某条属"次要条款"而漏报。
  - **高**：逐条从严，所有可争取的修改都提出（适合高风险或一次性交易）
  - **中**：核心底线条款（付款、违约、解除、争议解决）强制改；其余不利条款须列入报告，修改建议可从宽
  - **低**：仅对重大风险与违法/无效条款强制改，其余不利条款仍须列入报告作提示
- **(c) 业务图景**：把第 2 步总结呈现给用户，请其确认或修正。

**动态待确认点**（第 2 步列出的，逐条提问）：
- 把每个「待确认点」抛给用户，等其答复；若答复又带出新疑点，追加提问，直到无悬而未决的影响审查走向的问题。

全部确认后才进入审查。用户未回复时不得自行假定推进；用户答复不全的，就缺失部分再次询问。

### 第 4 步：基于知识库逐项审查
**4.0 条款清单抽取（强制前置，防跳读）**：先调用
```bash
$PY scripts/extract_clauses.py --input 原合同.docx --output clauses.json
```
把合同所有条款抽成清单（条号 + 主旨 + 原文片段，`para_idx` 与 `Document.paragraphs` 对齐）。然后**逐条**对照 playbook 判断是否命中检查点，每条都必须明确判定"命中/不命中"，不得整段跳过；只有逐条过完清单，才进入下面的阶段式复核。漏判一条款即视为审查不完整。

读 `references/contract-review-playbook.md`，严格按「整体 → 内容 → 格式」三阶段逐项检查，不得跳过阶段：
1. **整体审核**：立场判断、合同框架（交易类型 / 谁给谁什么 / 怎么给何时给 / 不给怎么办）
2. **内容审核**：合同主体 → 权利义务 → 款项与标的物 → 违约责任（从严）→ 特殊条款 → 争议解决 → 生效送达
3. **格式与程序审核**：合同名称、编号、错别字、附件、交叉引用
  此处格式仅指合同内容的格式，不包括文件格式。

**对抗视角（贯穿全条款）**：审查每类条款时执行 playbook「审核总原则 #5」——假设对方严格按此条款行权、我方处于最不利情形，检验我方是否被实质损害；尤其付款节点（预付款比例 / 发货前付清比例 / 质保金）、质保保修（起算点 / 期限 / 运费 / 隐蔽瑕疵）、保密（双向对等）、终止与续约（自动续约 / 解约后果）、转包等条款，不得仅作"是否清晰"的中性检查。

每条命中要点给出：**条款名 / 风险说明 / 修改建议 / 理由**，并按合同目的与谈判地位综合定优先级（高/中/低）。

### 第 5 步：生成修订合同
把审查出的修改点编为 `edits.json`，调用：
```bash
$PY scripts/track_changes.py --input 原合同.docx --edits edits.json --output 修订合同.docx
```
`edits.json` 格式：
```json
[
  {"action":"replace","para_idx":5,"old":"原文","new":"新文","comment":"建议改为X。理由：Y"},
  {"action":"insert","para_idx":8,"text":"新增条款","comment":"建议补充。理由：Y"},
  {"action":"comment","para_idx":3,"comment":"此处模糊。理由：Y"}
]
```
- `para_idx` 为 0 起始的段落索引（含标题段，与 `Document.paragraphs` 对齐）。
- `replace`：段落内 `old` 子串替换为 `new`，用 `w:del`+`w:ins` 标记；`old` 必须能在该段落纯文本中精确匹配到。
- `insert`：在 `para_idx` 段落之后插入一个新段落，整段标记为 `w:ins`。
- `comment`：对该段落加批注，格式统一「建议+理由」。
- 修订标记为**真实 OOXML 元素**（w:ins / w:del / comments.xml part），非样式模拟。

**发业务禁区（硬约束）**：修订合同（revised.docx）直接发给业务方，`new`/`text` 与 `comment` 一律不得出现内部知识库来源引用——禁用「审核手册」「知识库」「playbook」「审查要点」「审核经验积累」「依据手册」「参照手册」「见手册」等表述。
- `new`/`text`：必须是可直接入合同的纯条款文字，不含审查说明或来源标注。
- `comment`：理由只用业务与法律风险语言（如「金额表述模糊易引发争议」），不得写「依据审核手册第 X 点」「参照知识库」之类。
- `track_changes.py` 会机械扫描禁用词，命中即拒绝生成并报错——须改写 `edits.json` 后重跑，不得绕过。

### 第 6 步：生成审核报告
把审查结果编为 `review_result.json`，调用：
```bash
$PY scripts/generate_report.py --input review_result.json --output 审核报告.docx
```
`review_result.json` 格式：
```json
{
  "contract_name": "货物买卖合同",
  "review_date": "2026-08-09",
  "our_role": "甲方（买方）",
  "negotiation_position": "弱势方",
  "overall_risk": {"level": "中", "reason": "..."},
  "clauses": [
    {"priority":"高","name":"标的物条款","risk":"...","suggestion":"...","reason":"..."}
  ],
  "todos": ["向业务确认...","向业务确认..."]
}
```
报告三部分（缺一算失败）：①整体风险等级（高/中/低+理由）②条款建议按高/中/低优先级分组 ③待确认事项 TODO。

### 第 7 步：回补知识库 + 请用户确认 + 未命中复盘
提炼本次审核中可复用的检查要点，编为 `playbook_points.json`，调用：
```bash
$PY scripts/update_playbook.py --input playbook_points.json --playbook references/contract-review-playbook.md
```
`playbook_points.json` 格式：
```json
{
  "review_date": "2026-08-09",
  "points": [
    {"section": "（四）违约责任", "check": "违约责任须双向约定且计算口径明确"}
  ]
}
```
- `section` 须能匹配 playbook 中 `### ` 小节标题的子串（如「（四）违约责任」）。
- 脚本只追加不删除，追加到对应小节的「#### 审核经验积累」子节；同小节多次回补自动累积。
- **脚本运行后会把已写入条目打印到 stdout，必须原样反馈给用户并要求确认**；用户不认可的可手动从 playbook 删除该条（此为唯一允许的"修改"，且由用户主导）。
- **未命中复盘（自愈机制）**：回补后自检——若「整体风险等级=高」但命中条款数明显偏少，或用户反馈有遗漏，须主动复盘本次漏审的条款类型，提炼为检查点经用户确认后回补 playbook（`update_playbook.py` 已支持去重，重复不会写入）。目标：把这次漏掉的，下次能抓到。

### 第 8 步：交付三份产出
向用户交付：
1. **修订合同**（.docx，带 Word 修订标记与批注）
2. **审核报告**（.docx，三部分齐全）
3. **知识库已更新提示**：列出本次回补的检查项，标注「待用户确认」

## 死规矩

- **不许 mock XML 操作**——修订标记必须是真实 OOXML 元素（w:ins / w:del / w:delText / comments.xml）。
- **playbook.md 原有内容只许追加不许改、不许删**（update_playbook.py 已保证：diff 验证 0 删除行）。
- **每个脚本独立可运行**，接受命令行参数，不依赖外部状态。
- **合同输入仅 .docx**；审核报告输出 .docx；知识库回补直接追加到 playbook.md。
- 连败 3 次换项；结果比开工差就回滚并如实报告。
- 涉及身份证号等敏感信息一律由用户本人填写，本 skill 不落盘。

## 验收要点（脚本正确性自检）

- `track_changes.py`：输出 docx 的 XML 须含 `w:ins` 或 `w:del`；对未修订的原文档跑同样检查应不含。
- `generate_report.py`：输出 docx 须含「整体风险等级 / 条款修改建议 / 待确认事项」三个章节标题。
- `update_playbook.py`：备份后运行，`diff` 只有 `>` 追加行、无 `<` 删除行；剥离追加块后与原始内容字节一致。
- `extract_clauses.py`：对一份含多级条款的真实 `.docx` 合同运行，须输出条/款合计 ≥40，且含「第一条」「1.4」等标记。

